#pragma once
#include <string>

class Elingtemplate {
public:
    // Constructor
    Elingtemplate();

    // Destructor
    ~Elingtemplate();

    // Function to create a new template from a directory
    void createTemplate(const std::string& directory);
};



